package example

package CalculatorSecond

import scala.io.StdIn.readLine
import example.CalculatorSecond.ChaseSecnd

object CalculatorSecond extends App {
  //  val chs: ChaseSecnd = new ChaseSecnd

  /*  print("Choose operation ( * / + - ) ")
  chs.ch(readLine())

  print("First number ")
  chs.getNum1(readLine())

  print("Second number ")
  num2 = readLine()

  ch match {
    case "+" => result = num1.toInt + num2.toInt
    case "-" => result = num1.toInt - num2.toInt
    case "*" => result = num1.toInt * num2.toInt
    case "/" => result = num1.toInt / num2.toInt
    case _   => println("Invalid operation")
  }

  println("Result: " + result)
   */
}
